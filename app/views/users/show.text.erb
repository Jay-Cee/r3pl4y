<div class="user">
	<!-- USER-INFO -->
	<%= render :partial => 'users/user_info.html.erb', :object => @user %>

	<!-- USER-FOLLOW -->
	<%= render :partial => 'users/user_follow.html.erb', :object => @user %>

	<!-- USER-STATUS -->
	<%= render :partial => 'users/user_status.html.erb', :object => @user %>

	<ul class="review-list" data-source="<%= url_for(:controller => 'reviews', :action => 'index', :user_id => @user.name) %>">
	<!-- Dynamic loading of reviews from javascript -->	
	</ul>
</div>
